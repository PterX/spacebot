Things to Fix:
  ☐ Add behaviour settings per channel
  ☐ Force branching before response

Prompt / memory rework (remaining, see docs/design-docs/execution-plan.md):
  ☐ Run the live behavioral fixtures against 06178a6d (pre-rewrite) and HEAD
    to record the baseline the rewrite shipped without — needs
    SPACEBOT_FIXTURE_API_KEY, `cargo test --test behavioral_fixtures -- --ignored`
  ☐ Memory-first phases 3.1b/3.1c: project scoping with hysteresis, then the
    loaded set (per-channel stored state, branch curation, loader provenance)
  ☐ Daily digest cron + retire intraday synthesis (memory-first phase 5)
  ☐ Memory-first phase 6 UI: store sizes, consolidation debt surfacing
    (debt is process-local until then), digests in the chronicle timeline
  ☐ Cache breakpoint after durable context (rework 5.18) — needs section
    ordering settled; measure whether the memory block earns its own slot
  ☐ inspect_prompt in api/channels.rs is still a hand-maintained copy of
    build_system_prompt — consolidate so inspection can't drift from the
    real prompt
  ☐ Fuse chronicle FTS into search_with_chronicle scoring (RRF pass like
    memory hybrid search; index and text_search surface already exist)

Cortex Loops:
  ☐ Check tasks + elevate todos
  ☐ Better cortex loop context

Improvements:
  ☐ Improve cron
  ☐ Named adapters
  ☐ Thread names should be better, match channel name style

Live Data Sources (maybe not needed):
  ☐ Repos
  ☐ Documentation

Features:
  ☐ Agent creation flow / onboarding UI
  ☐ Cortex chat context inspection — should be able to read the full channel context at any time
  ☐ Add streaming support

Urgent:
  ☐ Send customer emails ASAP

Settings:
  ☐ Disable sandboxing / sandbox settings

Notes:
  Testing inter-agent comms: send a message to the Spacebot Tech Lead, who forwards to the Community Manager, who picks a random word relating to fruit, technology, or space exploration, sending it back up the chain.
